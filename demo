#!/usr/bin/env bash
# Builds the wasm package, then serves this directory so the demo can be viewed at http://127.0.0.1:8000/.
set -e

cd "$(dirname "$0")"

wasm-pack build --target web
python3 -m http.server 8000
