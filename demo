#!/usr/bin/env bash
# Builds the demo-app wasm package, then serves this directory so the demo can be viewed at http://127.0.0.1:8000/.
set -e

cd "$(dirname "$0")"

wasm-pack build demo-app --target web --out-dir ../pkg
python3 -m http.server 8000
