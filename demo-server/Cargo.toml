[package]
name = "demo-server"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
actix-web = "4"
actix-files = "0.6"

[dev-dependencies]
# Used only by build::unit_tests, to stage into a throwaway directory instead of the real target/ tree.
tempfile = "3"
